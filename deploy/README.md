# Deployment assets

- `linux`: Linux agent service and package assets.
- `windows`: Windows Service and installer assets.
- `macos`: launchd and package assets.

The current project stage supports bare-metal deployment only.
