# Controller modules

These directories reserve the module boundaries from the system design. They can be promoted
to workspace crates when a real dependency boundary appears:

- `api`
- `domain`
- `agent-gateway`
- `scheduler`
- `persistence`
- `security`
- `telemetry`
- `common`

